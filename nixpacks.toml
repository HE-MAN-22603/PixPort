[phases.setup]
nixPkgs = ['python310']

[phases.install]
cmds = ['pip install --no-cache-dir -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 60 app:app'
