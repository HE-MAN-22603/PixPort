steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:$COMMIT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:latest'
      - '.'
    id: 'build-image'

  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:$COMMIT_SHA'
    id: 'push-image-sha'
    waitFor: ['build-image']

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:latest'
    id: 'push-image-latest'
    waitFor: ['build-image']

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'pixport-app'
      - '--image'
      - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '1'
      - '--max-instances'
      - '10'
      - '--concurrency'
      - '80'
      - '--timeout'
      - '300s'
      - '--set-env-vars'
      - 'FLASK_ENV=production,UPLOAD_FOLDER=/tmp/uploads,PROCESSED_FOLDER=/tmp/processed,REMBG_MODEL=isnet-general-use'
      - '--execution-environment'
      - 'gen2'
    id: 'deploy-to-cloud-run'
    waitFor: ['push-image-sha', 'push-image-latest']

# Build options for optimization
options:
  # Use high-performance machine for faster builds
  machineType: 'E2_HIGHCPU_8'
  
  # Enable Docker layer caching for faster subsequent builds
  substitutionOption: 'ALLOW_LOOSE'
  
  # Log streaming for real-time feedback
  logging: 'CLOUD_LOGGING_ONLY'

# Substitutions (can be overridden)
substitutions:
  _REGION: 'us-central1'
  _PROJECT_ID: 'pixport-468609'
  _REPO_NAME: 'pixport-images'
  _SERVICE_NAME: 'pixport-app'

# Build timeout (increased for first builds with model downloads)
timeout: '1800s'  # 30 minutes

# Images to be pushed to registry
images:
  - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:$COMMIT_SHA'
  - 'us-central1-docker.pkg.dev/pixport-468609/pixport-images/pixport-app:latest'

# Tags for the build
tags:
  - 'pixport-flask-app'
  - 'cloud-run-deployment'
