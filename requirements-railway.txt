# Railway-optimized requirements for PixPort
# Optimized for fast build times and memory constraints

# Core Flask (lightweight versions)
Flask>=2.3.3,<4.0.0
Werkzeug>=2.3.7
Flask-Limiter>=3.5.0

# Image processing (minimal dependencies)
Pillow>=10.0.0
opencv-python-headless>=4.8.0.76

# Background removal (Railway-optimized)
rembg>=2.0.60
onnxruntime>=1.15.1
numpy>=1.24.3

# Utilities
python-dotenv>=1.0.0
psutil>=5.9.0
requests>=2.28.0

# Production server
gunicorn>=21.2.0

# Optional (Railway may not have Redis)
redis>=4.5.0

# Remove heavy dependencies that may cause timeouts
# reportlab>=4.0.0  # Commented out - not critical for Railway
# onnx>=1.14.0      # Commented out - included in onnxruntime
